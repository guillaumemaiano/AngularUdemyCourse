import { FontColorClassDirective } from './font-color-class.directive';

describe('FontColorClassDirective', () => {
  it('should create an instance', () => {
    const directive = new FontColorClassDirective();
    expect(directive).toBeTruthy();
  });
});
