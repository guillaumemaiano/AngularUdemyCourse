import { CssClassDirective } from './css-class.directive';

describe('CssClassDirective', () => {
  it('should create an instance', () => {
    const directive = new CssClassDirective();
    expect(directive).toBeTruthy();
  });
});
